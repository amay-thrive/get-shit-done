# ── Supabase (database + auth + realtime + vector memory) ──
# Live project: sundial-os (ap-south-1)
NEXT_PUBLIC_SUPABASE_URL=https://xuzcmtkcqfvicgfqoyal.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_...
# Dashboard → Project Settings → API keys → secret key (server-only, never expose)
SUPABASE_SECRET_KEY=sb_secret_...

# ── Anthropic — agent reasoning engine (required for agents) ──
ANTHROPIC_API_KEY=sk-ant-...

# ── Vercel cron auth (any long random string; Vercel sends it automatically) ──
CRON_SECRET=generate-a-long-random-string

# ── Optional: semantic memory embeddings (falls back to keyword search) ──
OPENAI_API_KEY=sk-...

# ── Optional integrations — settings page lights up as you add these ──
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
ZOHO_CLIENT_ID=
ZOHO_CLIENT_SECRET=
ZOHO_REFRESH_TOKEN=
ZOHO_WEBHOOK_SECRET=
ZAPIER_WEBHOOK_SECRET=
